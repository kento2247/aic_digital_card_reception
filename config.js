// Default config (can be overridden by localStorage)
const DEFAULT_CONFIG = {
    // Default API base URL (changeable via Settings in the UI)
    apiUrl: 'https://mypage-demo.tkento1985.workers.dev',
    apiKey: ''
};
